<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.jhta.repository.FriendsRequestDao">
    <insert id="insertFriendsRequest" parameterType="FriendsRequest">
        <selectKey keyProperty="id" resultType="int" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into friend_requests (
            sender_id, recipient_id, status
        ) values (
            #{senderId}, #{recipientId}, #{status}
        )
    </insert>

    <select id="checkIfExists" parameterType="int" resultType="boolean">
        select exists (
            select
                1
            from
                friend_requests
            where
                sender_id = #{sender-id} and recipient_id = #{recipient-id}
        ) as exist
    </select>
</mapper>